name = "b2b-api"
main = "index.js"
compatibility_date = "2024-01-01"
# NB: les routes workers (*/api/mollie* + */api/b2b* sur les 6 zones) sont déjà
# posées sur Cloudflare et stables — volontairement PAS re-déclarées ici : le
# token CI (CLOUDFLARE_API_TOKEN) n'a pas le scope zones/workers_routes:edit
# et wrangler échoue en Authentication error s'il tente de les réécrire.
# Pour changer le routage: action fondateur (dashboard CF ou token à jour).

[vars]
SUPABASE_URL = "https://rswdmjtdzrucqzzukfmd.supabase.co"

# Secret SUPABASE_SERVICE_KEY → wrangler secret put SUPABASE_SERVICE_KEY
# Secret MOLLIE_API_KEY → wrangler secret put MOLLIE_API_KEY
