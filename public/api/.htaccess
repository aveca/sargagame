# Force PHP execution in /api/ directory
# Fixes track-open.php / track-click.php returning PHP source instead of executing
AddHandler application/x-httpd-php .php

# Security: deny access to .php files directly if needed
# <FilesMatch "\.php$">
#     Require all granted
# </FilesMatch>